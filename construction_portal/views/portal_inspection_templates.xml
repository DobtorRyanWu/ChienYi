<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============================================
         自主檢查列表 /construction/<pid>/inspections
         ============================================ -->
    <template id="portal_construction_inspections" name="Construction Inspections">
        <t t-call="web.frontend_layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <div class="cy-v10-app">
                <t t-call="construction_portal.portal_v10_header"/>
                <div class="cy-breadcrumb">
                    <a t-attf-href="/construction/#{project.id}" class="cy-breadcrumb-item">首頁</a>
                    <span class="cy-breadcrumb-item active">自主檢查</span>
                </div>

                <div class="cy-content">
                    <div class="cy-sec-divider"><span>自主檢查</span></div>

                    <!-- 全寬新增按鈕 -->
                    <a t-attf-href="/construction/#{project.id}/inspection/new"
                       class="cy-btn-gradient-full amber" style="text-decoration: none; display: block;">
                        + 新增檢查
                    </a>

                    <!-- 批次匯入入口 -->
                    <a t-attf-href="/construction/#{project.id}/inspections/import"
                       class="cy-btn"
                       style="display: block; margin-top: 8px; padding: 10px; text-align: center; border: 1px solid rgba(255,255,255,0.18); color: var(--wb-t2); font-weight: 600; border-radius: 10px; text-decoration: none;">
                        <i class="fa fa-upload"/> 批次匯入總表單
                    </a>

                    <!-- 總筆數 -->
                    <div class="cy-total-count">共 <t t-esc="inspection_count"/> 筆</div>

                    <!-- 預約式通報單篩選 chips -->
                    <t t-if="slip_list">
                        <div class="cy-filter-pills" style="margin-bottom: 12px;">
                            <a t-attf-href="/construction/#{project.id}/inspections"
                               t-attf-class="cy-filter-pill #{not slip_filter and 'active' or ''}">全部</a>
                            <t t-foreach="slip_list" t-as="sl">
                                <a t-attf-href="/construction/#{project.id}/inspections?slip_id=#{sl.id}"
                                   t-attf-class="cy-filter-pill #{slip_filter == sl.id and 'active' or ''}">
                                    第 <t t-esc="sl.slip_no"/> 次
                                </a>
                            </t>
                        </div>
                    </t>

                    <t t-if="not inspections">
                        <div class="cy-empty">
                            <i class="fa fa-check-square-o"/>
                            <div class="cy-empty-text">尚無自主檢查紀錄</div>
                        </div>
                    </t>

                    <!-- 檢查卡片（border-left + checked count）-->
                    <t t-foreach="inspections" t-as="insp">
                        <a t-attf-href="/construction/inspection/#{insp.id}" class="cy-insp-card"
                           t-att-data-state="insp.state">
                            <div class="cy-insp-card-header">
                                <div>
                                    <div class="cy-insp-card-title">
                                        <t t-esc="insp.sub_project_name or insp.inspection_type_id.name or insp.name"/>
                                    </div>
                                    <div class="cy-insp-card-sub">
                                        <t t-esc="insp.inspection_location or ''"/> / <t t-esc="insp.inspection_date"/>
                                    </div>
                                </div>
                                <span t-attf-class="cy-badge cy-badge-#{insp.state}">
                                    <t t-esc="dict(insp._fields['state'].selection).get(insp.state, insp.state)"/>
                                </span>
                            </div>
                            <div class="cy-insp-card-footer">
                                <span>✓ <t t-esc="len(insp.checklist_ids.filtered(lambda l: l.check_result in ('pass', 'defect', 'na')))"/>/<t t-esc="len(insp.checklist_ids)"/> 項</span>
                                <t t-if="insp.inspection_timing">
                                    <span><t t-esc="dict(insp._fields['inspection_timing'].selection).get(insp.inspection_timing, '')"/></span>
                                </t>
                            </div>
                        </a>
                    </t>

                    <div class="d-flex justify-content-center mt-3">
                        <t t-call="portal.pager"/>
                    </div>
                </div>

                <t t-call="construction_portal.portal_bottom_nav_v10"/>
            </div>
        </t>
    </template>

    <!-- ============================================
         自主檢查詳情 /construction/inspection/<iid>
         ============================================ -->
    <template id="portal_construction_inspection_detail" name="Inspection Detail">
        <t t-call="web.frontend_layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <div class="cy-v10-app">
                <t t-call="construction_portal.portal_v10_header"/>
                <div class="cy-breadcrumb">
                    <a t-attf-href="/construction/#{project.id}" class="cy-breadcrumb-item">首頁</a>
                    <a t-attf-href="/construction/#{project.id}/inspections" class="cy-breadcrumb-item">檢查</a>
                    <span class="cy-breadcrumb-item active"><t t-esc="inspection.name"/></span>
                </div>

                <div class="cy-content">
                    <!-- 成功訊息 -->
                    <t t-if="request.params.get('message') == 'created'">
                        <div class="cy-event-card cy-event-info" style="margin-bottom: 12px;">
                            <span class="cy-event-icon" style="color: var(--wb-green);"><i class="fa fa-check-circle"/></span>
                            <div class="cy-event-body">
                                <div class="cy-event-title" style="color: var(--wb-green);">檢查建立成功</div>
                            </div>
                        </div>
                    </t>

                    <!-- 標題 + 狀態 -->
                    <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px;">
                        <div style="min-width: 0; flex: 1;">
                            <h4 style="font-weight: 700; margin: 0;">
                                <t t-esc="inspection.sub_project_name or inspection.name"/>
                            </h4>
                            <div style="font-size: 0.85rem; color: var(--wb-t3);">
                                <t t-esc="inspection.inspection_type_id.name or ''"/>
                            </div>
                        </div>
                        <span t-attf-class="cy-badge cy-badge-#{inspection.state}" style="flex-shrink: 0; margin-left: 8px;">
                            <t t-esc="dict(inspection._fields['state'].selection).get(inspection.state, inspection.state)"/>
                        </span>
                    </div>

                    <!-- 綜合結果（一般式）-->
                    <t t-if="inspection.overall_result">
                        <div t-attf-class="cy-event-card #{inspection.overall_result == 'pass' and 'cy-event-info' or inspection.overall_result == 'fail' and 'cy-event-urgent' or 'cy-event-warn'}" style="margin-bottom: 12px;">
                            <span class="cy-event-icon">
                                <i t-attf-class="fa #{inspection.overall_result == 'pass' and 'fa-check-circle' or inspection.overall_result == 'fail' and 'fa-times-circle' or 'fa-exclamation-circle'}"
                                   t-attf-style="color: #{inspection.overall_result == 'pass' and 'var(--wb-green)' or inspection.overall_result == 'fail' and 'var(--wb-red)' or 'var(--wb-amber)'}"/>
                            </span>
                            <div class="cy-event-body">
                                <div class="cy-event-title" t-attf-style="color: #{inspection.overall_result == 'pass' and 'var(--wb-green)' or inspection.overall_result == 'fail' and 'var(--wb-red)' or 'var(--wb-amber)'}">
                                    綜合結果：<t t-esc="result_selection.get(inspection.overall_result, inspection.overall_result)"/>
                                </div>
                                <div class="cy-event-desc" t-if="inspection.defect_count">
                                    <t t-esc="inspection.defect_count"/> 項缺失
                                </div>
                            </div>
                        </div>
                    </t>

                    <!-- 基本資訊 -->
                    <div class="cy-section-title">檢查資訊</div>
                    <div class="cy-info-section">
                        <div class="cy-info-row">
                            <span class="cy-info-label">檢查日期</span>
                            <span class="cy-info-value"><t t-esc="inspection.inspection_date"/></span>
                        </div>
                        <div class="cy-info-row">
                            <span class="cy-info-label">檢查位置</span>
                            <span class="cy-info-value"><t t-esc="inspection.inspection_location or '-'"/></span>
                        </div>
                        <div class="cy-info-row">
                            <span class="cy-info-label">檢查時機</span>
                            <span class="cy-info-value">
                                <t t-esc="timing_selection.get(inspection.inspection_timing, '-')"/>
                            </span>
                        </div>
                        <div class="cy-info-row">
                            <span class="cy-info-label">檢查人員</span>
                            <span class="cy-info-value"><t t-esc="inspection.inspector_id.name or '-'"/></span>
                        </div>
                        <div class="cy-info-row" t-if="inspection.contractor_company_id">
                            <span class="cy-info-label">承攬廠商</span>
                            <span class="cy-info-value"><t t-esc="inspection.contractor_company_id.name"/></span>
                        </div>
                    </div>

                    <!-- 檢查項目（按 stage 分群）-->
                    <t t-if="stage_groups">
                        <div class="cy-section-title">
                            <span>檢查項目</span>
                        </div>

                        <!-- 計數統計列 -->
                        <div class="cy-insp-counts" style="display: flex; gap: 12px; margin-bottom: 12px; flex-wrap: wrap;">
                            <span class="cy-badge" style="background: rgba(34,179,87,0.12); color: var(--wb-green);">
                                ✓ 合格 <span class="insp-count-pass">0</span>
                            </span>
                            <span class="cy-badge" style="background: rgba(232,54,79,0.12); color: var(--wb-red);">
                                ✗ 缺失 <span class="insp-count-defect">0</span>
                            </span>
                            <span class="cy-badge" style="background: rgba(139,144,160,0.12); color: var(--wb-t3);">
                                — N/A <span class="insp-count-na">0</span>
                            </span>
                        </div>

                        <t t-foreach="stage_groups.items()" t-as="sg">
                            <div style="font-size: 0.85rem; font-weight: 700; color: var(--wb-blue); margin: 12px 0 6px; padding-left: 4px;">
                                <t t-esc="sg[1]['label']"/>
                            </div>
                            <t t-foreach="sg[1]['items']" t-as="item">
                                <div class="cy-card" style="margin-bottom: 8px;">
                                    <div class="cy-card-body" style="padding: 12px 14px;">
                                        <div style="font-weight: 700; font-size: 0.9rem; margin-bottom: 4px;">
                                            <t t-esc="item.check_item"/>
                                        </div>
                                        <t t-if="item.design_standard">
                                            <div style="font-size: 0.8rem; color: var(--wb-t3); margin-bottom: 6px;">
                                                標準：<t t-esc="item.design_standard"/>
                                            </div>
                                        </t>
                                        <t t-if="item.actual_result">
                                            <div style="font-size: 0.85rem; color: var(--wb-t2); margin-bottom: 6px;">
                                                實際：<t t-esc="item.actual_result"/>
                                            </div>
                                        </t>
                                        <div>
                                            <span t-if="item.check_result == 'pass'"
                                                  class="cy-badge" style="background: rgba(34,179,87,0.12); color: var(--wb-green);">
                                                ✓ 合格
                                            </span>
                                            <span t-elif="item.check_result == 'defect'"
                                                  class="cy-badge" style="background: rgba(232,54,79,0.12); color: var(--wb-red);">
                                                ✗ 缺失
                                            </span>
                                            <span t-elif="item.check_result == 'na'"
                                                  class="cy-badge" style="background: rgba(139,144,160,0.12); color: var(--wb-t3);">
                                                — 無此項
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </t>
                    </t>

                    <!-- 備註 -->
                    <t t-if="inspection.note">
                        <div class="cy-section-title">備註</div>
                        <div class="cy-info-section" style="padding: 12px 16px;">
                            <div style="font-size: 0.9rem; white-space: pre-wrap;"><t t-esc="inspection.note"/></div>
                        </div>
                    </t>

                    <!-- 共用照片區塊 -->
                    <t t-call="construction_portal.portal_construction_photos_block"/>

                    <!-- 訊息紀錄 -->
                    <div class="cy-section-title" style="margin-top: 24px;">訊息</div>
                    <div style="background: var(--wb-bg2); border-radius: var(--wb-radius); border: 1px solid var(--wb-bdr); padding: 12px; margin-bottom: 14px;">
                        <t t-call="portal.message_thread">
                            <t t-set="object" t-value="inspection"/>
                        </t>
                    </div>
                </div>

                <t t-call="construction_portal.portal_bottom_nav_v10"/>
            </div>
        </t>
    </template>

    <!-- ============================================
         自主檢查新增表單 /construction/<pid>/inspection/new
         ============================================ -->
    <template id="portal_construction_inspection_form" name="Inspection Form">
        <t t-call="web.frontend_layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <div class="cy-v10-app">
                <t t-call="construction_portal.portal_v10_header"/>
                <div class="cy-breadcrumb">
                    <a t-attf-href="/construction/#{project.id}" class="cy-breadcrumb-item">首頁</a>
                    <a t-attf-href="/construction/#{project.id}/inspections" class="cy-breadcrumb-item">檢查</a>
                    <span class="cy-breadcrumb-item active">新增</span>
                </div>

                <div class="cy-content">
                    <h4 style="font-weight: 700; margin-bottom: 16px;">新增自主檢查</h4>

                    <form action="/construction/inspection/create" method="post" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <input type="hidden" name="project_id" t-att-value="project.id"/>

                        <!-- 檢查類型 -->
                        <div style="margin-bottom: 16px;">
                            <label class="form-label" style="font-size: 0.9rem; font-weight: 600;">
                                檢查類型 <span style="color: var(--wb-red);">*</span>
                            </label>
                            <select name="inspection_type_id" class="form-select" required="required" id="inspection_type_select">
                                <option value="">-- 選擇檢查類型 --</option>
                                <t t-foreach="inspection_types" t-as="itype">
                                    <option t-att-value="itype.id">
                                        <t t-esc="itype.name"/>
                                        <t t-if="itype.code"> (<t t-esc="itype.code"/>)</t>
                                    </option>
                                </t>
                            </select>
                        </div>

                        <!-- 分項工程名稱 -->
                        <div style="margin-bottom: 16px;">
                            <label class="form-label" style="font-size: 0.9rem; font-weight: 600;">
                                分項工程名稱 <span style="color: var(--wb-red);">*</span>
                            </label>
                            <input type="text" name="sub_project_name" class="form-control"
                                   placeholder="例：A 區鋼筋綁紮" required="required"/>
                        </div>

                        <!-- 日期 + 時機 -->
                        <div style="display: flex; gap: 12px; margin-bottom: 16px;">
                            <div style="flex: 1;">
                                <label class="form-label" style="font-size: 0.85rem;">檢查日期</label>
                                <input type="date" name="inspection_date" class="form-control"
                                       t-att-value="today" required="required"/>
                            </div>
                            <div style="flex: 1;">
                                <label class="form-label" style="font-size: 0.85rem;">檢查時機</label>
                                <select name="inspection_timing" class="form-select">
                                    <t t-foreach="timing_options" t-as="opt">
                                        <option t-att-value="opt[0]"
                                                t-att-selected="opt[0] == 'during' and 'selected' or None">
                                            <t t-esc="opt[1]"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                        </div>

                        <!-- 位置 -->
                        <div style="margin-bottom: 16px;">
                            <label class="form-label" style="font-size: 0.85rem;">檢查位置</label>
                            <input type="text" name="inspection_location" class="form-control"
                                   placeholder="具體施工位置"/>
                        </div>

                        <!-- 檢查項目（AJAX 動態載入）-->
                        <div class="cy-section-title">
                            <span>檢查項目</span>
                            <span style="font-size: 0.8rem; color: var(--wb-t3);" id="checklist_summary">
                                選擇類型後載入
                            </span>
                        </div>
                        <div id="checklist_container">
                            <div class="cy-event-card cy-event-info" style="margin-bottom: 12px;">
                                <span class="cy-event-icon" style="color: var(--wb-blue);"><i class="fa fa-info-circle"/></span>
                                <div class="cy-event-body">
                                    <div class="cy-event-desc">
                                        選擇檢查類型後自動載入檢查項目，可直接填寫各項結果。
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- 備註 -->
                        <div class="cy-section-title">備註</div>
                        <div style="margin-bottom: 16px;">
                            <textarea name="note" class="form-control" placeholder="備註說明..."></textarea>
                        </div>

                        <!-- 施工照片 -->
                        <div class="cy-section-title" style="cursor: pointer;" data-collapse-toggle="sec_insp_photos">
                            <span><i class="fa fa-camera" style="margin-right: 6px;"/>檢查照片</span>
                            <i class="fa fa-chevron-up" style="font-size: 0.8rem; color: var(--wb-t3);"/>
                        </div>
                        <div id="sec_insp_photos" style="margin-bottom: 16px;">
                            <div style="margin-bottom: 10px;">
                                <input type="file" name="photos" accept="image/*" multiple="multiple"
                                       class="form-control" style="padding: 10px;"/>
                            </div>
                            <div style="margin-bottom: 10px;">
                                <label class="form-label" style="font-size: 0.85rem;">說明(共用)</label>
                                <input type="text" name="photo_description" class="form-control"
                                       placeholder="這批照片的說明"/>
                            </div>
                            <div style="display: flex; gap: 8px; margin-bottom: 10px;">
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">分類</label>
                                    <select name="photo_category" class="form-select">
                                        <option value="">--</option>
                                        <t t-foreach="photo_categories or []" t-as="opt">
                                            <option t-att-value="opt[0]"><t t-esc="opt[1]"/></option>
                                        </t>
                                    </select>
                                </div>
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">拍攝地點</label>
                                    <input type="text" name="photo_location_description" class="form-control"
                                           placeholder="位置說明"/>
                                </div>
                            </div>
                            <div style="display: flex; gap: 8px; margin-bottom: 10px;">
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">緯度</label>
                                    <input type="text" name="photo_latitude" class="form-control cy-photos-lat"/>
                                </div>
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">經度</label>
                                    <input type="text" name="photo_longitude" class="form-control cy-photos-lng"/>
                                </div>
                            </div>
                            <button type="button" class="cy-btn cy-btn-outline"
                                    style="font-size: 0.8rem; padding: 6px 14px; min-height: 36px;"
                                    onclick="cyGetGeo(this);">
                                <i class="fa fa-crosshairs"/> 抓取目前位置
                            </button>
                        </div>

                        <!-- 提交 -->
                        <div style="margin-top: 20px; margin-bottom: 20px;">
                            <button type="submit" class="cy-btn cy-btn-amber cy-btn-block" style="font-size: 1.1rem;">
                                <i class="fa fa-save"/> 儲存檢查表
                            </button>
                        </div>
                    </form>
                </div>

                <t t-call="construction_portal.portal_bottom_nav_v10"/>
            </div>
        </t>
    </template>

    <!-- ============================================
         預約式自主檢查列表 /construction/<pid>/reservation-inspections
         ============================================ -->
    <template id="portal_construction_reservation_inspections" name="Reservation Inspections">
        <t t-call="web.frontend_layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <div class="cy-v10-app">
                <t t-call="construction_portal.portal_v10_header"/>
                <div class="cy-breadcrumb">
                    <a t-attf-href="/construction/#{project.id}" class="cy-breadcrumb-item">首頁</a>
                    <span class="cy-breadcrumb-item active">預約式檢查</span>
                </div>

                <div class="cy-content">
                    <h4 style="font-weight: 700; margin-bottom: 12px;">預約式自主檢查</h4>

                    <!-- 通報單篩選 chips -->
                    <t t-if="slip_list">
                        <div class="cy-filter-pills" style="margin-bottom: 12px;">
                            <a t-attf-href="/construction/#{project.id}/reservation-inspections"
                               t-attf-class="cy-filter-pill #{not slip_filter and 'active' or ''}">全部</a>
                            <t t-foreach="slip_list" t-as="s">
                                <a t-attf-href="/construction/#{project.id}/reservation-inspections?slip_id=#{s.id}"
                                   t-attf-class="cy-filter-pill #{slip_filter and int(slip_filter) == s.id and 'active' or ''}">
                                    <t t-esc="s.slip_number or s.name"/>
                                </a>
                            </t>
                        </div>
                    </t>

                    <t t-if="not inspections">
                        <div class="cy-empty">
                            <i class="fa fa-clipboard-check"/>
                            <div class="cy-empty-text">尚無預約式檢查</div>
                            <div style="font-size: 0.85rem; color: var(--wb-t3); margin-top: 4px;">
                                請從通報單詳情頁新增
                            </div>
                        </div>
                    </t>

                    <t t-foreach="inspections" t-as="insp">
                        <a t-attf-href="/construction/reservation-inspection/#{insp.id}"
                           class="cy-card" style="text-decoration: none; color: inherit; display: block; margin-bottom: 8px;">
                            <div class="cy-card-body">
                                <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 4px;">
                                    <span style="font-weight: 700;"><t t-esc="insp.sub_project_name or insp.inspection_no"/></span>
                                    <span t-attf-class="cy-badge cy-badge-#{insp.state}">
                                        <t t-esc="dict(insp._fields['state'].selection).get(insp.state, insp.state)"/>
                                    </span>
                                </div>
                                <div style="font-size: 0.85rem; color: var(--wb-t3);">
                                    <t t-esc="insp.inspection_date"/> · <t t-esc="insp.inspection_type_id.name or ''"/>
                                </div>
                                <div t-if="insp.slip_id" style="font-size: 0.8rem; color: var(--wb-blue); margin-top: 4px;">
                                    <i class="fa fa-link"/> <t t-esc="insp.slip_id.slip_number or insp.slip_id.name"/>
                                </div>
                            </div>
                        </a>
                    </t>

                    <div class="d-flex justify-content-center mt-3">
                        <t t-call="portal.pager"/>
                    </div>
                </div>

                <t t-call="construction_portal.portal_bottom_nav_v10"/>
            </div>
        </t>
    </template>

    <!-- ============================================
         預約式自主檢查表單 /construction/<pid>/reservation-inspection/new?slip_id=
         ============================================ -->
    <template id="portal_construction_reservation_inspection_form" name="Reservation Inspection Form">
        <t t-call="web.frontend_layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <div class="cy-v10-app">
                <t t-call="construction_portal.portal_v10_header"/>
                <div class="cy-breadcrumb">
                    <a t-attf-href="/construction/#{project.id}" class="cy-breadcrumb-item">首頁</a>
                    <a t-attf-href="/construction/#{project.id}/slip/#{slip.id}" class="cy-breadcrumb-item">通報單</a>
                    <span class="cy-breadcrumb-item active">新增檢查</span>
                </div>

                <div class="cy-content">
                    <h4 style="font-weight: 700; margin-bottom: 4px;">新增預約式檢查</h4>
                    <div style="font-size: 0.85rem; color: var(--wb-t3); margin-bottom: 16px;">
                        所屬通報單:<strong><t t-esc="slip.slip_number or slip.name"/></strong>
                    </div>

                    <form action="/construction/reservation-inspection/create" method="post" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <input type="hidden" name="slip_id" t-att-value="slip.id"/>

                        <div style="margin-bottom: 16px;">
                            <label class="form-label" style="font-size: 0.9rem; font-weight: 600;">
                                檢查類型 <span style="color: var(--wb-red);">*</span>
                            </label>
                            <select name="inspection_type_id" class="form-select" required="required">
                                <option value="">-- 選擇 --</option>
                                <t t-foreach="inspection_types" t-as="itype">
                                    <option t-att-value="itype.id"><t t-esc="itype.name"/></option>
                                </t>
                            </select>
                            <div style="font-size: 0.75rem; color: var(--wb-t3); margin-top: 4px;">
                                儲存後將自動載入該類型預設檢查項目
                            </div>
                        </div>

                        <div style="margin-bottom: 16px;">
                            <label class="form-label" style="font-size: 0.9rem; font-weight: 600;">
                                分項工程名稱 <span style="color: var(--wb-red);">*</span>
                            </label>
                            <input type="text" name="sub_project_name" class="form-control" required="required"/>
                        </div>

                        <div style="display: flex; gap: 12px; margin-bottom: 16px;">
                            <div style="flex: 1;">
                                <label class="form-label" style="font-size: 0.85rem;">檢查日期</label>
                                <input type="date" name="inspection_date" class="form-control"
                                       t-att-value="today" required="required"/>
                            </div>
                            <div style="flex: 1;">
                                <label class="form-label" style="font-size: 0.85rem;">檢查時機</label>
                                <select name="inspection_timing" class="form-select">
                                    <t t-foreach="timing_options" t-as="opt">
                                        <option t-att-value="opt[0]"
                                                t-att-selected="opt[0] == 'during' and 'selected' or None">
                                            <t t-esc="opt[1]"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                        </div>

                        <div style="margin-bottom: 16px;">
                            <label class="form-label" style="font-size: 0.85rem;">檢查位置</label>
                            <input type="text" name="inspection_location" class="form-control"
                                   placeholder="具體施工位置"/>
                        </div>

                        <div style="display: flex; gap: 12px; margin-bottom: 16px;">
                            <div style="flex: 1;">
                                <label class="form-label" style="font-size: 0.85rem;">承攬廠商</label>
                                <input type="text" name="contractor_name" class="form-control"/>
                            </div>
                            <div style="flex: 1;">
                                <label class="form-label" style="font-size: 0.85rem;">協力廠商</label>
                                <input type="text" name="subcontractor_name" class="form-control"/>
                            </div>
                        </div>

                        <div class="cy-section-title">備註</div>
                        <div style="margin-bottom: 16px;">
                            <textarea name="note" class="form-control" placeholder="備註說明..."></textarea>
                        </div>

                        <!-- 施工照片 -->
                        <div class="cy-section-title" style="cursor: pointer;" data-collapse-toggle="sec_resv_insp_photos">
                            <span><i class="fa fa-camera" style="margin-right: 6px;"/>檢查照片</span>
                            <i class="fa fa-chevron-up" style="font-size: 0.8rem; color: var(--wb-t3);"/>
                        </div>
                        <div id="sec_resv_insp_photos" style="margin-bottom: 16px;">
                            <div style="margin-bottom: 10px;">
                                <input type="file" name="photos" accept="image/*" multiple="multiple"
                                       class="form-control" style="padding: 10px;"/>
                            </div>
                            <div style="margin-bottom: 10px;">
                                <label class="form-label" style="font-size: 0.85rem;">說明(共用)</label>
                                <input type="text" name="photo_description" class="form-control"/>
                            </div>
                            <div style="display: flex; gap: 8px; margin-bottom: 10px;">
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">分類</label>
                                    <select name="photo_category" class="form-select">
                                        <option value="">--</option>
                                        <t t-foreach="photo_categories or []" t-as="opt">
                                            <option t-att-value="opt[0]"><t t-esc="opt[1]"/></option>
                                        </t>
                                    </select>
                                </div>
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">拍攝地點</label>
                                    <input type="text" name="photo_location_description" class="form-control"/>
                                </div>
                            </div>
                            <div style="display: flex; gap: 8px; margin-bottom: 10px;">
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">緯度</label>
                                    <input type="text" name="photo_latitude" class="form-control cy-photos-lat"/>
                                </div>
                                <div style="flex: 1;">
                                    <label class="form-label" style="font-size: 0.85rem;">經度</label>
                                    <input type="text" name="photo_longitude" class="form-control cy-photos-lng"/>
                                </div>
                            </div>
                            <button type="button" class="cy-btn cy-btn-outline"
                                    style="font-size: 0.8rem; padding: 6px 14px; min-height: 36px;"
                                    onclick="cyGetGeo(this);">
                                <i class="fa fa-crosshairs"/> 抓取目前位置
                            </button>
                        </div>

                        <div style="margin-top: 20px; margin-bottom: 20px;">
                            <button type="submit" class="cy-btn cy-btn-amber cy-btn-block" style="font-size: 1.1rem;">
                                <i class="fa fa-save"/> 儲存檢查表
                            </button>
                        </div>
                    </form>
                </div>

                <t t-call="construction_portal.portal_bottom_nav_v10"/>
            </div>
        </t>
    </template>

    <!-- ============================================
         預約式自主檢查詳情 /construction/reservation-inspection/<id>
         ============================================ -->
    <template id="portal_construction_reservation_inspection_detail" name="Reservation Inspection Detail">
        <t t-call="web.frontend_layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <div class="cy-v10-app">
                <t t-call="construction_portal.portal_v10_header"/>
                <div class="cy-breadcrumb">
                    <a t-attf-href="/construction/#{project.id}" class="cy-breadcrumb-item">首頁</a>
                    <a t-attf-href="/construction/#{project.id}/reservation-inspections" class="cy-breadcrumb-item">預約式檢查</a>
                    <span class="cy-breadcrumb-item active"><t t-esc="inspection.inspection_no"/></span>
                </div>

                <div class="cy-content">
                    <t t-if="request.params.get('message') == 'created'">
                        <div class="cy-event-card cy-event-info" style="margin-bottom: 12px;">
                            <span class="cy-event-icon" style="color: var(--wb-green);"><i class="fa fa-check-circle"/></span>
                            <div class="cy-event-body">
                                <div class="cy-event-title" style="color: var(--wb-green);">檢查建立成功</div>
                            </div>
                        </div>
                    </t>

                    <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px;">
                        <div style="min-width: 0; flex: 1;">
                            <h4 style="font-weight: 700; margin: 0;">
                                <t t-esc="inspection.sub_project_name or inspection.inspection_no"/>
                            </h4>
                            <div style="font-size: 0.85rem; color: var(--wb-t3);">
                                <t t-esc="inspection.inspection_type_id.name or ''"/>
                            </div>
                        </div>
                        <span t-attf-class="cy-badge cy-badge-#{inspection.state}" style="flex-shrink: 0; margin-left: 8px;">
                            <t t-esc="dict(inspection._fields['state'].selection).get(inspection.state, inspection.state)"/>
                        </span>
                    </div>

                    <div class="cy-section-title">所屬通報單</div>
                    <div class="cy-info-section">
                        <div class="cy-info-row">
                            <span class="cy-info-label">通報單號</span>
                            <span class="cy-info-value">
                                <a t-attf-href="/construction/#{project.id}/slip/#{slip.id}">
                                    <t t-esc="slip.slip_number or slip.name"/>
                                </a>
                            </span>
                        </div>
                    </div>

                    <div class="cy-section-title">檢查資訊</div>
                    <div class="cy-info-section">
                        <div class="cy-info-row">
                            <span class="cy-info-label">檢查日期</span>
                            <span class="cy-info-value"><t t-esc="inspection.inspection_date"/></span>
                        </div>
                        <div class="cy-info-row">
                            <span class="cy-info-label">檢查時機</span>
                            <span class="cy-info-value">
                                <t t-esc="timing_selection.get(inspection.inspection_timing, '-')"/>
                            </span>
                        </div>
                        <div class="cy-info-row" t-if="inspection.inspection_location">
                            <span class="cy-info-label">檢查位置</span>
                            <span class="cy-info-value"><t t-esc="inspection.inspection_location"/></span>
                        </div>
                        <div class="cy-info-row" t-if="inspection.contractor_name">
                            <span class="cy-info-label">承攬廠商</span>
                            <span class="cy-info-value"><t t-esc="inspection.contractor_name"/></span>
                        </div>
                        <div class="cy-info-row" t-if="inspection.subcontractor_name">
                            <span class="cy-info-label">協力廠商</span>
                            <span class="cy-info-value"><t t-esc="inspection.subcontractor_name"/></span>
                        </div>
                        <div class="cy-info-row">
                            <span class="cy-info-label">填表人</span>
                            <span class="cy-info-value"><t t-esc="inspection.inspector_id.name or '-'"/></span>
                        </div>
                    </div>

                    <t t-if="stage_groups">
                        <div class="cy-section-title">檢查項目</div>
                        <t t-foreach="stage_groups.items()" t-as="sg">
                            <div style="font-size: 0.85rem; font-weight: 700; color: var(--wb-blue); margin: 12px 0 6px; padding-left: 4px;">
                                <t t-esc="sg[1]['label']"/>
                            </div>
                            <t t-foreach="sg[1]['items']" t-as="item">
                                <div class="cy-card" style="margin-bottom: 8px;">
                                    <div class="cy-card-body" style="padding: 12px 14px;">
                                        <div style="font-weight: 700; font-size: 0.9rem; margin-bottom: 4px;">
                                            <t t-esc="item.check_item"/>
                                        </div>
                                        <t t-if="item.design_standard">
                                            <div style="font-size: 0.8rem; color: var(--wb-t3); margin-bottom: 6px;">
                                                標準:<t t-esc="item.design_standard"/>
                                            </div>
                                        </t>
                                        <div>
                                            <span t-if="item.check_result == 'pass'"
                                                  class="cy-badge" style="background: rgba(34,179,87,0.12); color: var(--wb-green);">
                                                ✓ 合格
                                            </span>
                                            <span t-elif="item.check_result == 'defect'"
                                                  class="cy-badge" style="background: rgba(232,54,79,0.12); color: var(--wb-red);">
                                                ✗ 缺失
                                            </span>
                                            <span t-elif="item.check_result == 'na'"
                                                  class="cy-badge" style="background: rgba(139,144,160,0.12); color: var(--wb-t3);">
                                                — 無此項
                                            </span>
                                            <span t-else=""
                                                  class="cy-badge" style="background: rgba(139,144,160,0.12); color: var(--wb-t3);">
                                                未填
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </t>
                    </t>

                    <t t-if="inspection.note">
                        <div class="cy-section-title">備註</div>
                        <div class="cy-info-section" style="padding: 12px 16px;">
                            <div style="font-size: 0.9rem; white-space: pre-wrap;"><t t-esc="inspection.note"/></div>
                        </div>
                    </t>

                    <!-- 共用照片區塊 -->
                    <t t-call="construction_portal.portal_construction_photos_block"/>
                </div>

                <t t-call="construction_portal.portal_bottom_nav_v10"/>
            </div>
        </t>
    </template>

    <!-- ============================================
         自主檢查批次匯入 /construction/<pid>/inspections/import
         ============================================ -->
    <template id="portal_construction_inspection_import" name="Inspection Batch Import">
        <t t-call="web.frontend_layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <div class="cy-v10-app">
                <t t-call="construction_portal.portal_v10_header"/>
                <div class="cy-breadcrumb">
                    <a t-attf-href="/construction/#{project.id}" class="cy-breadcrumb-item">首頁</a>
                    <a t-attf-href="/construction/#{project.id}/inspections" class="cy-breadcrumb-item">自主檢查</a>
                    <span class="cy-breadcrumb-item active">批次匯入</span>
                </div>

                <div class="cy-content">
                    <div style="text-align: center; margin-bottom: 16px;">
                        <div style="font-size: 1.4rem; font-weight: 900; color: var(--wb-amber);">批次匯入自主檢查</div>
                        <div style="font-size: 0.9rem; color: var(--wb-t2); margin-top: 4px;">
                            上傳「自檢表總表單」XLSX 彙總檔
                        </div>
                    </div>

                    <t t-if="imported or skipped or failed">
                        <div style="margin-bottom: 14px; padding: 12px 14px; background: rgba(59,141,224,0.10); border-radius: 8px; font-size: 0.95rem; line-height: 1.7;">
                            <div style="font-weight: 700; margin-bottom: 6px;">
                                <i class="fa fa-info-circle"/> 匯入結果
                            </div>
                            <div style="color: var(--wb-green); font-weight: 700;">
                                <i class="fa fa-check-circle"/> 成功：<t t-esc="imported"/> 筆
                            </div>
                            <t t-if="skipped">
                                <div style="color: var(--wb-amber); font-weight: 700;">
                                    <i class="fa fa-forward"/> 跳過（重複）：<t t-esc="skipped"/> 筆
                                </div>
                            </t>
                            <t t-if="failed">
                                <div style="color: var(--wb-red); font-weight: 700;">
                                    <i class="fa fa-times-circle"/> 失敗：<t t-esc="failed"/> 筆
                                </div>
                            </t>
                        </div>

                        <t t-if="failed_details">
                            <div style="margin-bottom: 14px;">
                                <div class="cy-section-title">處理明細</div>
                                <div class="cy-info-section" style="padding: 12px 14px; font-size: 0.85rem; line-height: 1.6; max-height: 280px; overflow-y: auto;">
                                    <t t-foreach="failed_details" t-as="d">
                                        <div style="padding: 6px 0; border-bottom: 1px solid rgba(255,255,255,0.08);">
                                            <div style="color: var(--wb-t2); word-break: break-all;">
                                                <t t-esc="d.get('filename', '')"/>
                                            </div>
                                            <div style="color: var(--wb-red);">
                                                <t t-esc="d.get('error', '')"/>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </t>
                    </t>

                    <form t-attf-action="/construction/#{project.id}/inspections/import"
                          method="post" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                        <div class="cy-section-title">選擇檔案</div>
                        <div class="cy-info-section" style="padding: 14px 16px;">
                            <div style="margin-bottom: 14px;">
                                <label class="form-label" style="font-weight: 600;">XLSX 檔案（單檔）</label>
                                <input type="file" name="files" class="form-control"
                                       accept=".xlsx,.xlsm" required="required"/>
                                <small style="color: var(--wb-t3); display: block; margin-top: 6px;">
                                    來源：<code>12-文書資料-A標/07-施工抽查/…/自檢表總表單*.xlsx</code>
                                </small>
                            </div>

                            <div style="font-size: 0.85rem; color: var(--wb-t2); line-height: 1.6; margin-top: 12px; padding: 10px; background: rgba(0,0,0,0.12); border-radius: 6px;">
                                <div style="font-weight: 700; margin-bottom: 4px;">解析規則</div>
                                <ul style="margin: 0; padding-left: 20px;">
                                    <li>掃描符合 <code>N.名稱</code> 格式的所有 sheet（例：<code>1.測量放樣</code>、<code>6.混凝土</code>）</li>
                                    <li>每個 sheet 從 R5 起抽取：次數 / 檢查日期 / 檢查部位 / 備註</li>
                                    <li>sheet 名對應 <code>self.inspection.type</code>，找不到會自動建 stub</li>
                                    <li>同日期 + 類型 + 部位已有紀錄則 skip</li>
                                    <li>checklist 若無 default 項目，預設建 1 筆 placeholder</li>
                                    <li><strong style="color: var(--wb-amber);">POC 階段最多匯 30 筆</strong></li>
                                </ul>
                            </div>
                        </div>

                        <div style="display: flex; gap: 10px; margin-top: 18px; margin-bottom: 16px;">
                            <a t-attf-href="/construction/#{project.id}/inspections"
                               class="cy-btn"
                               style="flex: 1; padding: 16px; text-align: center; border: 1px solid rgba(255,255,255,0.18); color: var(--wb-t2); font-weight: 700; border-radius: 10px; text-decoration: none;">
                                取消
                            </a>
                            <button type="submit" class="cy-btn-gradient-full"
                                    style="flex: 2; padding: 16px; font-size: 1.1rem;">
                                開始匯入
                            </button>
                        </div>
                    </form>
                </div>

                <t t-call="construction_portal.portal_bottom_nav_v10"/>
            </div>
        </t>
    </template>
</odoo>
